package seedu.address.testutil;

import seedu.address.model.student.Note;

public class TypicalNote {
    public static final Note TYPICAL_STUDENT_NOTE = new NoteBuilder().withValue("Default note").build();
    public static final Note TYPICAL_STUDENT_NOTE_ALT = new NoteBuilder().withValue("Another default note").build();
}

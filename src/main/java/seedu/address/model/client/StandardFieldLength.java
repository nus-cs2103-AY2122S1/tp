package seedu.address.model.client;

/**
 * Represents the max limit of characters that a default field should have.
 */
public interface StandardFieldLength {
    /**
     * The default max length of a field.
     */
    int MAX_LENGTH = 30;
}

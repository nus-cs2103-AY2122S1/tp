package seedu.address.model.client;

/**
 * Represents the max limit of characters that a shorter field can have.
 */
public interface ShorterFieldLength extends StandardFieldLength {

    int MAX_LENGTH = 15;
}

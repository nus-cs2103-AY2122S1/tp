package seedu.address.model.client;

/**
 * Represents the max limit of characters that a longer field can have.
 */
public interface LongerFieldLength extends StandardFieldLength {

    int MAX_LENGTH = 100;
}
